---
phase: 01-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "User can navigate through all 4 steps using Continue and Back buttons"
    - "Progress bar shows correct state (completed dots with checkmarks, current dot larger, no ring)"
    - "Horizontal slide transition plays on every step change (~200ms, ease-out, no bounce)"
    - "Browser back button navigates between wizard steps without leaving /apply"
    - "Enter key advances placeholder steps (ReviewPayStep, DoneStep)"
    - "No Start Over button is visible on any step"
    - "URL stays at /apply during all navigation"
  artifacts: []
  key_links:
    - from: "src/app/apply/page.tsx"
      to: "all 4 step components"
      via: "STEPS array maps currentStep to component"
      pattern: "STEPS\[currentStep"
    - from: "src/components/ProgressBar.tsx"
      to: "src/app/apply/page.tsx"
      via: "currentStep prop drives visual state"
      pattern: "ProgressBar.*currentStep"
---

<objective>
End-to-end verification of the 4-step wizard shell.

Purpose: After Plan 01 (wizard restructure) and Plan 02 (ProgressBar fix) are complete, verify the full wizard works as a cohesive unit. This is a human-verification checkpoint — the executor should NOT run automated tests but instead walk through the manual verification steps.

Output: Confirmed working 4-step wizard skeleton. No code changes expected unless issues found.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Plan 01 output
src/app/apply/page.tsx — Restructured for 4 steps
src/app/apply/steps/ReviewPayStep.tsx — New placeholder
src/app/apply/steps/DoneStep.tsx — New placeholder

# Plan 02 output
src/components/ProgressBar.tsx — Ring removed, current dot larger
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete 4-step wizard skeleton with animated progress bar, slide transitions, browser history integration, and Enter key support</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` from project root
    2. Open http://localhost:3000/apply

    **Step 1 (Business Info):**
    - [ ] Progress bar shows 4 dots, first dot is larger (no ring), no checkmarks
    - [ ] Business form is visible with fields: Business Name, TIN, Nature of Business, Floor Area
    - [ ] No "Start Over" button visible
    - [ ] Continue button present, no Back button (first step)

    **Step 2 (Contact):**
    - [ ] Click Continue on Step 1 — slide transitions left-to-right (~200ms, no bounce)
    - [ ] Progress bar: dot 1 is blue with checkmark, dot 2 is larger (no ring)
    - [ ] Contact form visible with Phone Number and Email fields
    - [ ] Back and Continue buttons present

    **Step 3 (Review & Pay):**
    - [ ] Click Continue — slide transitions
    - [ ] Progress bar: dots 1-2 have checkmarks, dot 3 is larger
    - [ ] Placeholder visible: "Review & Pay" title, description text
    - [ ] Back and Continue buttons present

    **Step 4 (Done):**
    - [ ] Click Continue — slide transitions
    - [ ] Progress bar: dots 1-3 have checkmarks, dot 4 is larger
    - [ ] Placeholder visible: "Application Submitted" title, description text
    - [ ] Back button present, NO Continue button

    **Browser back:**
    - [ ] Press browser back button — navigates to Step 3
    - [ ] Press browser back again — navigates to Step 2
    - [ ] Press browser back again — navigates to Step 1
    - [ ] URL stays at /apply throughout

    **Enter key:**
    - [ ] On Step 3 (placeholder), press Enter — advances to Step 4
    - [ ] On Step 4 (placeholder), press Enter — does nothing (terminal step)

    **Back navigation:**
    - [ ] From Step 4, click Back — reverse slide animation, returns to Step 3
    - [ ] Continue clicking Back through all steps — reverse animations work

    **Mobile viewport:**
    - [ ] Resize browser to 375px width — layout remains centered, max-w-md
    - [ ] Buttons remain tappable, content not cut off
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- All checkbox items in how-to-verify pass
- No visual glitches, broken animations, or missing elements
- Browser back/forward works correctly
- Mobile layout is clean at 375px
</verification>

<success_criteria>
- [ ] All 4 steps render correctly
- [ ] Progress bar shows correct state at each step
- [ ] Slide transitions animate smoothly (no bounce, ~200ms)
- [ ] Browser back button works without leaving /apply
- [ ] Enter key works on placeholder steps
- [ ] Mobile layout is clean and centered
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
