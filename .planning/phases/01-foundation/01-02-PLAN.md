---
phase: 01-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ProgressBar.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-06

must_haves:
  truths:
    - "Completed step dots show blue fill with a white checkmark (Lucide Check icon)"
    - "Current step dot is visually larger than other dots (scale 1.3)"
    - "Current step dot does NOT have a ring around it"
    - "Progress fill line animates smoothly between dots when navigating"
    - "Progress bar displays exactly 4 dots"
  artifacts:
    - path: "src/components/ProgressBar.tsx"
      provides: "4-dot animated progress bar with completed/current visual states"
      min_lines: 50
  key_links:
    - from: "src/components/ProgressBar.tsx"
      to: "framer-motion"
      via: "motion.div for animated fill line and dot scaling"
      pattern: "motion\.div|AnimatePresence"
    - from: "src/components/ProgressBar.tsx"
      to: "lucide-react"
      via: "Check icon for completed step markers"
      pattern: "Check.*lucide"
---

<objective>
Refine the ProgressBar component to match the locked visual decisions from CONTEXT.md.

Purpose: The ProgressBar already has 4 dots and animated fill, but the current step has a ring (ring-2 ring-blue-600 ring-offset-2) which contradicts the locked decision: "Current: larger dot (not ring)". The fill line animation also needs to be confirmed as smooth. This plan fixes these visual discrepancies.

Output: Updated `src/components/ProgressBar.tsx` with corrected current-step styling.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Current implementation
src/components/ProgressBar.tsx — Has ring on current step (line 50-51), needs removal

# Locked decisions (from user prompt):
- 4 dots, no labels, white header strip
- Completed: blue fill + white checkmark (Lucide Check)
- Current: larger dot (not ring)
- Animated fill line
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ring from current step dot</name>
  <files>src/components/ProgressBar.tsx</files>
  <action>
    In `src/components/ProgressBar.tsx`, remove the ring styling from the current step dot.

    Current code (around line 50):
    ```
    isCurrent && 'ring-2 ring-blue-600 ring-offset-2'
    ```

    Remove this conditional class entirely. The current step should be identified only by:
    - `scale: 1.3` (already present in the motion animate prop)
    - `backgroundColor: '#2563EB'` (already present)

    The ring creates visual clutter and contradicts the "larger dot (not ring)" locked decision.

    Also verify:
    - Completed dots still show blue fill + white Lucide Check icon ✓ (already correct)
    - Fill line animates between dots ✓ (already correct with motion width)
    - Total dots = 4 ✓ (already correct)
  </action>
  <verify>
    - Visually inspect: current step dot should be larger but have NO ring
    - TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>Current step dot is visually larger (scale 1.3) with no ring. Completed dots show checkmark. Fill line animates smoothly.</done>
</task>

</tasks>

<verification>
- Run `npx tsc --noEmit` — must pass
- Visual check: navigate to /apply, confirm current dot is larger without ring
- Verify completed dots show white checkmark on blue background
- Verify fill line animates between dots on step change
</verification>

<success_criteria>
- [ ] Current step dot has no ring (no ring-2, ring-blue-600, or ring-offset-2 classes)
- [ ] Current step dot is scaled to 1.3x
- [ ] Completed dots show Lucide Check icon in white on blue background
- [ ] Fill line animates smoothly between dots
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
