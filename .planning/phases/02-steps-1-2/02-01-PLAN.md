---
phase: 02-steps-1-2
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validation.ts
  - src/app/apply/steps/BusinessInfoStep.tsx
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04
  - BIZ-05
  - LOC-01
  - LOC-02
  - LOC-03
  - LOC-04
  - LOC-05
  - LOC-06
user_setup: []

must_haves:
  truths:
    - "User sees Personal, Business, and Location section headings in Step 1"
    - "User can enter Full Name of Assured (Personal field)"
    - "User can enter Street Address (Location field)"
    - "User can select Region, Province, City/Municipality, Barangay in cascading order"
    - "Selecting a parent clears child selections silently"
    - "Location dropdowns are disabled until parent is selected"
    - "All fields validate on blur with red text below (not red border)"
    - "Required fields show asterisk (*) on labels"
    - "Business info fields (businessName, tin, natureOfBusiness, floorArea with 'sqm' suffix) remain functional"
    - "Continue button submits combined form and proceeds to Step 2"
  artifacts:
    - path: "src/lib/validation.ts"
      provides: "Combined Zod schema with fullName, streetAddress, and business + location fields"
      contains: "businessInfoSchema with fullName and streetAddress"
    - path: "src/app/apply/steps/BusinessInfoStep.tsx"
      provides: "Combined Business Info + Location form (Step 1)"
      contains: "Section headings (Personal, Business, Location)"
      min_lines: 180
  key_links:
    - from: "BusinessInfoStep.tsx"
      to: "validation.ts"
      via: "zodResolver(businessInfoSchema) combined schema"
      pattern: "zodResolver.*businessInfoSchema"
    - from: "BusinessInfoStep.tsx"
      to: "psgc-data.ts"
      via: "loadPSGCData + cascading useMemo filters"
      pattern: "loadPSGCData|getProvinces|getCities|getBarangays"
    - from: "BusinessInfoStep.tsx"
      to: "useApplicationStore.ts"
      via: "setBusinessInfo + setLocation on submit"
      pattern: "setBusinessInfo|setLocation"
---

<objective>
Merge LocationStep fields into BusinessInfoStep to create a single Step 1 form with Personal (Full Name), Business (businessName, tin, natureOfBusiness, floorArea), and Location (Street Address + cascading PSGC dropdowns) sections.

Purpose: Phase 2 requires Step 1 = Business Info + Location combined. Currently these are separate steps. This plan merges them into one validated form.

Output: Updated BusinessInfoStep.tsx with three section groups and combined validation. Updated validation.ts with fullName and streetAddress fields added to businessInfoSchema.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-steps-1-2/02-CONTEXT.md
@src/app/apply/steps/BusinessInfoStep.tsx
@src/app/apply/steps/LocationStep.tsx
@src/lib/validation.ts
@src/lib/psgc-data.ts
@src/store/useApplicationStore.ts
@src/components/ui/select.tsx
@src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update businessInfoSchema with fullName and streetAddress</name>
  <files>src/lib/validation.ts</files>
  <action>
    Add two fields to the existing `businessInfoSchema`:
    1. `fullName` — `z.string().min(2, 'Full name is required (2-100 characters)').max(100, 'Full name is required (2-100 characters)')` (Personal field, not Business)
    2. `streetAddress` — `z.string().min(1, 'Street address is required')` (Location field)

    Keep all existing fields (businessName, tin, natureOfBusiness, floorArea) unchanged. Do NOT modify locationSchema or contactSchema.

    Export updated `BusinessInfoFormData` type (auto-derived from schema via z.infer).
  </action>
  <verify>
    TypeScript compiles: `npx tsc --noEmit`. The businessInfoSchema has 6 fields (fullName, businessName, tin, floorArea, natureOfBusiness, streetAddress). The inferred type BusinessInfoFormData includes fullName and streetAddress.
  </verify>
  <done>
    businessInfoSchema validates fullName (2-100 chars), streetAddress (required), and all 4 existing business fields. BusinessInfoFormData type includes all 6 fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Merge location fields into BusinessInfoStep</name>
  <files>src/app/apply/steps/BusinessInfoStep.tsx</files>
  <action>
    Refactor BusinessInfoStep to be the combined Step 1 form with three sections. Extract the approach from LocationStep.tsx (PSGC cascading dropdowns with useMemo, useEffect, handleBlur, locationSchema validation) into this component.

    **Section structure (in order):**

    1. **Personal** (`<h3>Personal</h3>`)
       - Full Name of Assured: Input, placeholder "Juan Dela Cruz", error from errors.fullName

    2. **Business** (`<h3>Business</h3>`)
       - Business Name: Input (existing)
       - TIN: Input (existing)
       - Nature of Business: Select (existing, 24 categories)
       - Floor Area: Input with 'sqm' suffix (existing)

    3. **Location** (`<h3>Location</h3>`)
       - Street Address: Input, placeholder "123 Main St", error from errors.streetAddress
       - Region: Select dropdown with PSGC data (from LocationStep pattern)
       - Province: Select dropdown, disabled until region selected
       - City/Municipality: Select dropdown, disabled until province selected
       - Barangay: Select dropdown, disabled until city selected

    **Location implementation (ported from LocationStep.tsx):**
    - useState for PSGC data loading, isLoading, location errors, touched state
    - useEffect to call loadPSGCData() on mount
    - useMemo for regions, provinces, cities, barangays (using store's location codes)
    - Cascading change handlers: parent clears children via setLocation({...})
    - Blur validation using locationSchema.safeParse(location)
    - Red text error display below each Select (not red border)
    - Disabled state: loading disables Region; empty parent disables children

    **Form behavior:**
    - useForm with zodResolver(businessInfoSchema) — covers Personal + Business fields
    - Location fields validated separately via locationSchema (same pattern as LocationStep)
    - On submit: validate both businessInfoSchema AND locationSchema. If both pass, call setBusinessInfo(data) and onNext().
    - Required fields show asterisk on labels: "Full Name of Assured *", "Business Name *", etc.
    - max-w-md mx-auto space-y-6 layout (existing)

     **PSGC Dropdowns — Searchable/Combobox:**
     - Region, Province, City/Municipality, and Barangay dropdowns use a combobox pattern per CONTEXT.md ("Searchable dropdowns (combobox pattern)")
     - Add a filter/search input at the top of each dropdown list so users can type to narrow options
     - Filtering is client-side: match against option label text, case-insensitive
     - The combobox behavior can be added as a prop or wrapper around the existing Select component (e.g., a `SearchableSelect` variant) without modifying the base Select component itself

     **Do NOT:**
     - Modify LocationStep.tsx (it stays as-is for reference)
     - Add toast notifications on parent clear (silent clear per CONTEXT.md)
     - Change the base Select component's existing API

    Reference: LocationStep.tsx lines 28-149 for PSGC loading, cascading, and validation patterns.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Step 1 renders three sections: Personal, Business, Location
    - Personal section has Full Name field
    - Business section has all 4 existing fields
    - Location section has Street Address + 4 cascading PSGC dropdowns
    - Each PSGC dropdown has a searchable/filter input for combobox pattern
    - Typing in filter narrows options case-insensitively
    - Selecting Region enables Province dropdown and loads options
    - Selecting Region clears Province, City, Barangay silently
    - Blur on any field shows validation error as red text below
    - Continue button validates both businessInfoSchema and locationSchema
  </verify>
  <done>
    BusinessInfoStep renders all fields from Personal, Business, and Location sections. PSGC cascading works identically to LocationStep. Validation on blur for all fields. Form submit validates combined schemas. Layout matches max-w-md mx-auto space-y-6.
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- BusinessInfoStep renders 3 section headings (Personal, Business, Location)
- Full Name field accepts input and validates on blur (min 2 chars)
- Street Address field accepts input and validates on blur (required)
- Cascading PSGC: Region → Province → City → Barangay works
- Parent selection clears children silently
- Children disabled until parent selected
- Business fields (businessName, tin, natureOfBusiness, floorArea) still functional
- Floor Area shows 'sqm' suffix
- Continue button validates all fields before proceeding
</verification>

<success_criteria>
- [ ] validation.ts: businessInfoSchema has fullName and streetAddress fields
- [ ] BusinessInfoStep.tsx: renders Personal section with Full Name
- [ ] BusinessInfoStep.tsx: renders Business section with 4 existing fields
- [ ] BusinessInfoStep.tsx: renders Location section with Street Address + 4 PSGC dropdowns
- [ ] BusinessInfoStep.tsx: PSGC dropdowns use combobox pattern with search/filter input
- [ ] BusinessInfoStep.tsx: cascading PSGC dropdowns with lazy load and parent-clears-children
- [ ] BusinessInfoStep.tsx: blur validation with red text below inputs
- [ ] BusinessInfoStep.tsx: asterisk (*) on required field labels
- [ ] BusinessInfoStep.tsx: submit validates businessInfoSchema + locationSchema
- [ ] TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-steps-1-2/02-01-SUMMARY.md`
</output>
