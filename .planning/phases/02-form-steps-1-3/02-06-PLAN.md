---
phase: 02-form-steps-1-3
plan: "06"
type: execute
wave: 3
depends_on:
  - "03"
  - "04"
  - "05"
files_modified:
  - src/app/apply/page.tsx
autonomous: false
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04
  - BIZ-05
  - LOC-01
  - LOC-02
  - LOC-03
  - LOC-04
  - LOC-05
  - LOC-06
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06

must_haves:
  truths:
    - "User sees a step indicator showing current step (1, 2, 3) out of total"
    - "User sees the correct step component based on currentStep in store"
    - "User can navigate forward (Continue button) and backward (Back button)"
    - "Step transitions are smooth — no page reload"
    - "Progress indicator reflects current position"
    - "All three step components are wired to the same Zustand store"
    - "Mobile-first centered layout (max-w-md) with consistent spacing"
  artifacts:
    - path: "src/app/apply/page.tsx"
      provides: "Application wizard page with step navigation"
      contains: "Step indicator, step renderer, navigation logic"
  key_links:
    - from: "src/app/apply/page.tsx"
      to: "src/store/useApplicationStore.ts"
      via: "currentStep, nextStep, prevStep from useApplicationStore"
      pattern: "useApplicationStore.*(currentStep|nextStep|prevStep)"
    - from: "src/app/apply/page.tsx"
      to: "src/app/apply/steps/BusinessInfoStep.tsx"
      via: "Renders BusinessInfoStep when currentStep === 1"
      pattern: "BusinessInfoStep"
    - from: "src/app/apply/page.tsx"
      to: "src/app/apply/steps/LocationStep.tsx"
      via: "Renders LocationStep when currentStep === 2"
      pattern: "LocationStep"
    - from: "src/app/apply/page.tsx"
      to: "src/app/apply/steps/ContactStep.tsx"
      via: "Renders ContactStep when currentStep === 3"
      pattern: "ContactStep"
---

<objective>
Create the wizard page that hosts all three step components with step indicator navigation.

Purpose: Ties the three steps together into a cohesive multi-step form experience. This is the integration layer.

Output: `src/app/apply/page.tsx`
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Prerequisites (from Plans 03-05 SUMMARYs):
- `src/app/apply/steps/BusinessInfoStep.tsx` — Step 1
- `src/app/apply/steps/LocationStep.tsx` — Step 2
- `src/app/apply/steps/ContactStep.tsx` — Step 3

Prerequisites (from Plan 01 SUMMARY):
- `src/store/useApplicationStore.ts` — currentStep, nextStep, prevStep
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wizard page with step indicator</name>
  <files>src/app/apply/page.tsx</files>
  <action>
    Create `src/app/apply/page.tsx` as a 'use client' page component.

    Implementation:
    1. Import `useApplicationStore` from `@/store/useApplicationStore`
    2. Import all three step components
    3. Define step metadata:
    ```typescript
    const STEPS = [
      { label: 'Business', component: BusinessInfoStep },
      { label: 'Location', component: LocationStep },
      { label: 'Contact', component: ContactStep },
    ] as const;
    ```

    Step indicator (horizontal, above the form):
    ```typescript
    const StepIndicator = () => (
      <div className="flex items-center justify-center gap-2 mb-8">
        {STEPS.map((step, i) => (
          <div key={i} className="flex items-center gap-2">
            <div className={cn(
              'w-8 h-8 rounded-full flex items-center justify-center text-sm font-medium',
              currentStep === i + 1
                ? 'bg-[#2563EB] text-white'
                : currentStep > i + 1
                  ? 'bg-green-500 text-white'
                  : 'bg-gray-200 text-gray-500'
            )}>
              {currentStep > i + 1 ? '✓' : i + 1}
            </div>
            <span className={cn(
              'text-sm hidden sm:inline',
              currentStep === i + 1 ? 'text-[#2563EB] font-medium' : 'text-gray-400'
            )}>
              {step.label}
            </span>
            {i < STEPS.length - 1 && (
              <div className={cn(
                'w-8 h-0.5',
                currentStep > i + 1 ? 'bg-green-500' : 'bg-gray-200'
              )} />
            )}
          </div>
        ))}
      </div>
    );
    ```

    Step renderer:
    ```typescript
    const CurrentStepComponent = STEPS[currentStep - 1]?.component;
    ```

    Page layout:
    ```tsx
    <div className="min-h-screen bg-gray-50">
      <div className="max-w-md mx-auto px-4 py-8">
        <StepIndicator />
        <CurrentStepComponent
          onNext={nextStep}
          onBack={prevStep}
          isFirstStep={currentStep === 1}
          isLastStep={currentStep === STEPS.length}
        />
      </div>
    </div>
    ```

    Import `cn` from `@/lib/utils`.
  </action>
  <verify>
    Page renders. Step indicator shows 3 steps. Current step highlighted in #2563EB. Completed steps show green checkmark. Clicking through all 3 steps works. Navigation forwards and backwards works.
  </verify>
  <done>
    Wizard page with step indicator, step rendering, and forward/backward navigation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete wizard flow</name>
  <files>src/app/apply/page.tsx</files>
  <action>
    After Task 1 completes, run `npm run dev` and manually verify the complete 3-step wizard:
    1. Visit /apply — step indicator shows step 1 active
    2. Fill in business fields — errors appear on blur if invalid
    3. Select nature of business from dropdown (24 options)
    4. Enter floor area — 'sqm' suffix visible
    5. Click Continue — step 2 loads with cascading dropdowns
    6. Select Region → Province populates. Change Region → Province/City/Barangay clear
    7. Complete all 4 location dropdowns
    8. Click Continue — step 3 loads with contact fields
    9. Enter phone/email — validation on blur
    10. Coverage and Premium auto-calculate based on floor area
    11. Click Back — returns to step 2 with data preserved
    12. Click Back again — returns to step 1 with data preserved
    13. Layout is centered max-w-md on mobile
  </action>
  <verify>
    All 13 verification steps pass without errors. Step transitions work. Data persists across steps. Coverage/premium calculate correctly.
  </verify>
  <done>
    Human confirms wizard works end-to-end. Type "approved" or describe issues found.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Step indicator renders 3 steps with correct active/completed states
- All 3 step components render correctly at their respective steps
- Forward and backward navigation preserves data
- Mobile-first max-w-md centered layout
- No page reloads during step transitions
</verification>

<success_criteria>
Wizard page exists, compiles, renders step indicator, navigates between all 3 steps, preserves data across transitions, and passes human verification of the complete flow.
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-steps-1-3/02-06-SUMMARY.md`
</output>
