---
phase: 02-form-steps-1-3
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/select.tsx
  - src/components/ui/input.tsx
autonomous: true
requirements:
  - LOC-03
  - BIZ-05

must_haves:
  truths:
    - "Select component renders a styled dropdown with trigger, value display, and scrollable options list"
    - "Select supports disabled state and placeholder text"
    - "Select uses Bethel blue (#2563EB) focus ring, no dark mode classes"
    - "Input component supports label, error message, icon slot, and inline suffix (e.g., 'sqm')"
    - "Input shows error text below the field in red"
    - "Input uses Bethel blue (#2563EB) focus ring, no dark mode classes"
  artifacts:
    - path: "src/components/ui/select.tsx"
      provides: "Reusable Select component for cascading PSGC dropdowns"
      exports: ["Select", "SelectTrigger", "SelectValue", "SelectContent", "SelectItem"]
    - path: "src/components/ui/input.tsx"
      provides: "Form input with label, error, icon, suffix support"
      exports: ["Input"]
  key_links:
    - from: "src/components/ui/select.tsx"
      to: "src/lib/utils.ts"
      via: "cn() for class merging"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "src/components/ui/input.tsx"
      to: "src/lib/utils.ts"
      via: "cn() for class merging"
      pattern: "import.*cn.*from.*@/lib/utils"
---

<objective>
Create two reusable UI primitives: a Select component for cascading PSGC dropdowns and an updated Input component with suffix support for the floor area 'sqm' display.

Purpose: Step components (Plans 03-05) need these primitives. Building them in Wave 1 allows parallel step development.

Output: `src/components/ui/select.tsx`, `src/components/ui/input.tsx`
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Reference patterns from:
- C:/Users/ampoy/Barangay Linkod Project/BarangayLinkodWebRegistration-main/BarangayLinkodWebRegistration-main/src/components/ui/Input.tsx
- C:/Users/ampoy/Barangay Linkod Project/BarangayLinkodWebRegistration-main/BarangayLinkodWebRegistration-main/src/app/register/steps/LocationStep.tsx

Project conventions: Next.js 16, Tailwind 4, mobile-first max-w-md, Bethel blue (#2563EB), NO dark mode.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Select component</name>
  <files>src/components/ui/select.tsx</files>
  <action>
    Create `src/components/ui/select.tsx` with a compound Select component.

    Components to export:
    - `Select`: wrapper, manages open/close state via internal useState. Props: `value: string`, `onValueChange: (value: string) => void`, `disabled?: boolean`, `children`
    - `SelectTrigger`: button that shows current value or placeholder. Props: `className?: string`, `children`
    - `SelectValue`: displays selected label or placeholder. Props: `placeholder?: string`
    - `SelectContent`: dropdown panel, positioned absolute below trigger, max-h-[300px] overflow-y-auto. Props: `className?: string`, `children`
    - `SelectItem`: individual option. Props: `value: string`, `className?: string`, `children`

    Styling (Tailwind, NO dark mode):
    - Trigger: `h-12 w-full rounded-lg border border-gray-300 bg-white px-4 text-sm text-left`
    - Focus ring: `focus:outline-none focus:ring-2 focus:ring-[#2563EB] focus:border-transparent`
    - Disabled: `disabled:cursor-not-allowed disabled:opacity-50`
    - Content: `absolute z-50 w-full mt-1 bg-white border border-gray-200 rounded-lg shadow-lg`
    - Item hover: `hover:bg-blue-50 cursor-pointer px-4 py-2 text-sm`

    Use React Context to share open/close state and value between compound components. Close on outside click (useEffect with mousedown listener). Close on item select.

    Reference the LocationStep.tsx pattern for how the Select is used in cascading dropdowns, but build a standalone component without shadcn dependency.
  </action>
  <verify>
    Component compiles. Renders with a trigger showing placeholder. Opens/closes on click. Selecting an item calls onValueChange and closes.
  </verify>
  <done>
    Select compound component exports 5 sub-components. Supports disabled, placeholder, value binding, and outside-click dismiss.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Input component with suffix support</name>
  <files>src/components/ui/input.tsx</files>
  <action>
    Create `src/components/ui/input.tsx` with a forwardRef Input component.

    Props (extending InputHTMLAttributes):
    - `label?: string`
    - `error?: string`
    - `icon?: ReactNode` (left icon slot)
    - `suffix?: string` (inline suffix like 'sqm', displayed after input text)

    Structure:
    ```
    <div className="space-y-2">
      {label && <label>...</label>}
      <div className="relative flex items-center">
        {icon && <div className="absolute left-3">icon</div>}
        <input ... />
        {suffix && <span className="ml-1 text-sm text-gray-500 whitespace-nowrap">{suffix}</span>}
      </div>
      {error && <p className="text-sm text-red-500">{error}</p>}
    </div>
    ```

    Styling (Tailwind, NO dark mode):
    - Input: `flex h-11 w-full rounded-lg border border-gray-300 bg-white px-4 py-2 text-sm`
    - Focus: `focus:outline-none focus:ring-2 focus:ring-[#2563EB] focus:border-transparent`
    - Error state: `border-red-500 focus:ring-red-500`
    - Icon padding: icon && `pl-10`
    - Label: `text-sm font-medium text-gray-700`

    Generate input id from label if not provided. Use `cn()` from `@/lib/utils` for class merging.
  </action>
  <verify>
    Component compiles. Renders label, accepts ref, shows error text, displays suffix after input.
  </verify>
  <done>
    Input component with label, error, icon, and suffix support. Uses #2563EB focus ring. No dark mode.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Select renders, opens/closes, selects items, handles disabled
- Input renders label, error, icon, suffix; forwardRef works
- Both use #2563EB focus ring, no dark mode classes
</verification>

<success_criteria>
Both UI components exist, compile, and follow project conventions (Bethel blue, no dark mode, mobile-first). Select supports cascading dropdown use case. Input supports floor area suffix display.
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-steps-1-3/02-02-SUMMARY.md`
</output>
