---
phase: 02-form-steps-1-3
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/app/apply/steps/BusinessInfoStep.tsx
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04
  - BIZ-05

must_haves:
  truths:
    - "User sees business name text input with label and error on blur"
    - "User sees TIN input with format hint and validates 123-456-789-000 pattern on blur"
    - "User sees nature of business dropdown with 24 categories"
    - "User sees floor area numeric input with inline 'sqm' suffix"
    - "Validation errors appear only on blur, not on keystroke"
    - "User can proceed to next step only when all fields are valid"
    - "Section label 'Business' groups the fields visually"
  artifacts:
    - path: "src/app/apply/steps/BusinessInfoStep.tsx"
      provides: "Business information form step (Step 1 of wizard)"
      contains: "24 nature of business categories as constant array"
  key_links:
    - from: "src/app/apply/steps/BusinessInfoStep.tsx"
      to: "src/store/useApplicationStore.ts"
      via: "useApplicationStore() for businessInfo state and setBusinessInfo"
      pattern: "useApplicationStore.*businessInfo"
    - from: "src/app/apply/steps/BusinessInfoStep.tsx"
      to: "src/lib/validation.ts"
      via: "businessInfoSchema with zodResolver for validation"
      pattern: "businessInfoSchema|zodResolver"
    - from: "src/app/apply/steps/BusinessInfoStep.tsx"
      to: "src/components/ui/input.tsx"
      via: "Input component for all form fields"
      pattern: "import.*Input"
---

<objective>
Build the BusinessInfoStep form component — the first step of the application wizard. Includes business name, TIN, nature of business (dropdown of 24 categories), and floor area (with 'sqm' suffix).

Purpose: First visible form step. Validates on blur using react-hook-form + Zod. Reads/writes Zustand store.

Output: `src/app/apply/steps/BusinessInfoStep.tsx`
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Prerequisites (from Plan 01 SUMMARY):
- `src/store/useApplicationStore.ts` — businessInfo state, setBusinessInfo
- `src/lib/validation.ts` — businessInfoSchema

Prerequisites (from Plan 02 SUMMARY):
- `src/components/ui/input.tsx` — Input with suffix support
- `src/components/ui/select.tsx` — Select component

Reference pattern: ProfileStep.tsx from BarangayLinkodWebRegistration (react-hook-form + zodResolver + Zustand store)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Nature of Business constant list</name>
  <files>src/app/apply/steps/BusinessInfoStep.tsx</files>
  <action>
    At the top of the file, define a constant array `NATURE_OF_BUSINESS_OPTIONS` with 24 business categories:

    ```typescript
    const NATURE_OF_BUSINESS_OPTIONS = [
      'Retail Trade',
      'Wholesale Trade',
      'Food Service / Restaurant',
      'Manufacturing',
      'Construction',
      'Transportation / Logistics',
      'Real Estate',
      'Professional Services',
      'Healthcare / Medical',
      'Education / Training',
      'Information Technology',
      'Financial Services',
      'Agriculture / Farming',
      'Fishing / Aquaculture',
      'Mining / Quarrying',
      'Tourism / Hospitality',
      'Entertainment / Recreation',
      'Personal Services',
      'Repair / Maintenance',
      'Cleaning / Sanitation',
      'Security Services',
      'Printing / Publishing',
      'Import / Export',
      'Other',
    ] as const;
    ```

    This is a flat list. Class I/II classification is internal only — not shown to user.
  </action>
  <verify>
    Array compiles. Contains exactly 24 items. Used as dropdown options.
  </verify>
  <done>
    24 business categories defined as const array.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build BusinessInfoStep form with blur validation</name>
  <files>src/app/apply/steps/BusinessInfoStep.tsx</files>
  <action>
    Create `src/app/apply/steps/BusinessInfoStep.tsx` as a 'use client' component.

    Props: `{ onNext: () => void; onBack: () => void; isFirstStep: boolean }`

    Implementation:
    1. Import `useForm` from react-hook-form, `zodResolver` from @hookform/resolvers/zod
    2. Import `businessInfoSchema` from `@/lib/validation`
    3. Import `useApplicationStore` from `@/store/useApplicationStore`
    4. Import `Input` from `@/components/ui/input`
    5. Import `Select, SelectTrigger, SelectValue, SelectContent, SelectItem` from `@/components/ui/select`

    Form setup:
    ```typescript
    const { businessInfo, setBusinessInfo } = useApplicationStore();
    const { register, handleSubmit, setValue, watch, formState: { errors }, trigger } = useForm({
      resolver: zodResolver(businessInfoSchema),
      defaultValues: businessInfo,
      mode: 'onBlur', // validation on blur only
    });
    ```

    Fields layout (section label "Business"):
    - Business Name: Input with `register('businessName')`, label "Business Name", placeholder "e.g., Juan Dela Cruz Store"
    - TIN: Input with `register('tin')`, label "TIN (Tax Identification Number)", placeholder "123-456-789-000"
    - Nature of Business: Select component, value from `watch('natureOfBusiness')`, onValueChange calls `setValue('natureOfBusiness', v)` then `trigger('natureOfBusiness')`. Options from NATURE_OF_BUSINESS_OPTIONS.
    - Floor Area: Input with `register('floorArea')`, label "Floor Area", placeholder "0", suffix "sqm", type "text" (not number, to allow decimal input)

    On submit: `setBusinessInfo(data); onNext()`

    Layout: `max-w-md mx-auto space-y-6`. Section heading `<h3 className="text-lg font-semibold">Business</h3>` before fields.
  </action>
  <verify>
    Component compiles. Renders 4 fields. Validation errors appear on blur (not on keystroke). Select shows 24 options. Floor area shows 'sqm' suffix. Submit calls setBusinessInfo and onNext.
  </verify>
  <done>
    BusinessInfoStep renders with all 4 fields, blur validation, store integration, and section label.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Component renders all 4 fields with correct labels
- Blur triggers validation (type invalid TIN, blur, see error)
- Select shows 24 nature of business options
- Floor area shows inline 'sqm' suffix
- Valid form submission calls setBusinessInfo and onNext
- Section label "Business" visible above fields
</verification>

<success_criteria>
BusinessInfoStep.tsx exists, compiles, renders 4 validated fields with blur-only validation, uses Zustand store, shows section label, and navigates on valid submit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-steps-1-3/02-03-SUMMARY.md`
</output>
